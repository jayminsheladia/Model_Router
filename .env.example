# Copy this file to .env and fill in your key to enable the real LLM
# classification fallback (app/llm_classifier.py). Without it, the router
# falls back to the heuristic classifier only -- everything else still works.
ANTHROPIC_API_KEY=
